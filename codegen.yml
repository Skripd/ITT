overwrite: true
schema: http://localhost:3000/graphql
documents: src/app/graphql/*.graphql
generates:
  ./src/app/generated/graphql.ts:
    plugins:
      - typescript-common
      - typescript-client
      - typescript-apollo-angular
  # ../itt-server/src/generated/graphql.ts:
  #   plugins:
  #     - typescript-common
  #     - typescript-client
  #     - typescript-apollo-angular
  ./schema.json:
    plugins:
      - introspection
  # ../itt-server/schema.json:
  #   plugins:
  #     - introspection